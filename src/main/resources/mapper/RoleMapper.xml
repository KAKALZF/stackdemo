<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ample16.stackdemo.mapper.RoleMapper">
    <sql id="Base_Column_List">
	FID,
	FNAME,
	FCREATE_TIME,
	FUPDATE_TIME,
	FSTATUS
</sql>
    <resultMap id="BaseResultMap" type="com.ample16.stackdemo.pojo.dto.RoleDo">
        <result column="FID" property="id"/>
        <result column="FNAME" property="name"/>
        <result column="FCREATE_TIME" property="createTime"/>
        <result column="FUPDATE_TIME" property="updateTime"/>
        <result column="FSTATUS" property="status"/>
    </resultMap>
    <insert id="add" parameterType="com.ample16.stackdemo.pojo.dto.RoleDo" useGeneratedKeys="true" keyProperty="id">
        <selectKey resultType="java.lang.Long" order="AFTER" keyProperty="id">
            SELECT SEQ_BDZS.CURRVAL AS ID FROM DUAL
        </selectKey>
        insert into T_BDZS_ROLE
        <trim prefix="(" suffix=")" suffixOverrides=",">
            FID,
            <if test="name != null">
                FNAME,
            </if>
            <if test="createTime != null">
                FCREATE_TIME,
            </if>
            <if test="updateTime != null">
                FUPDATE_TIME,
            </if>
            <if test="status != null">
                FSTATUS,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            SEQ_BDZS.nextval,
            <if test="name != null">
                #{name},
            </if>
            <if test="createTime != null">
                #{createTime},
            </if>
            <if test="updateTime != null">
                #{updateTime},
            </if>
            <if test="status != null">
                #{status},
            </if>
        </trim>
    </insert>

    <!--auto generated by MybatisCodeHelper on 2021-04-01-->
    <select id="findAllById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from T_BDZS_ROLE
        where FID=#{id}
    </select>

    <update id="update">
        update T_BDZS_ROLE
        <set>
            <if test="name!=null and name!=''">
                Fname=#{name}
            </if>
        </set>
        where FID=#{id}
    </update>
</mapper>
